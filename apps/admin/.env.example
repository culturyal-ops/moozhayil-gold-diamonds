# Admin portal — environment variables
# Copy to apps/admin/.env for local development.

# Leave empty when using Vite dev-server proxy (vite.config.ts proxies /v1 → :3000).
# Set to your API origin in staging/production builds, e.g. https://api.moozhayil.com
VITE_API_BASE=
